# way 1  | load shell confiugration
#source ~/.zshrc1 || true
#source ~/.bashrc || true

# load shell configuration
[[ -f ~/.zshrc ]] && source ~/.zshrc # MAC
[[ -f ~/.bashrc ]] && source ~/.bashrc # Ubuntu


ops-app version
